machine:
  node:
    version: 0.10.35
  services:
    - docker
dependencies:
  pre:
    - docker build -t mazlix/testci .
test:
  override:
    - docker run -d -p 80:80 mazlix/testci; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:80
